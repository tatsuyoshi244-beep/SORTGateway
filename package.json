{
  "name": "sort-gateway",
  "version": "2.1.0",
  "private": true,
  "description": "SORT Gateway monorepo",
  "scripts": {
    "dev": "npm run dev --prefix apps/enterprise",
    "build": "npm run build --prefix apps/enterprise",
    "start:enterprise": "npm run start --prefix apps/enterprise",
    "setup:legacy": "node server/scripts/setup-env.js",
    "legacy:server": "node server/index.js"
  }
}
